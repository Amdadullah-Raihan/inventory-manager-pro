import Sidebar from "../components/SideBar/Sidebar";

export default function Layout({ children }) {
    return (
        <>

            <main>

                {children}
            </main>

        </>
    )
}